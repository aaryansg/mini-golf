{
  "name": "unity-webgl-build",
  "version": "1.0.0",
  "description": "Unity WebGL build hosted on Railway",
  "scripts": {
    "start": "serve -s -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}